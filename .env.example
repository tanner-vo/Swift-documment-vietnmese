# Manual translation editing is disabled by default on public deployments.
NEXT_PUBLIC_ENABLE_TRANSLATION_EDITING=false

# Required when editing is enabled. Use a randomly generated value of at least
# 32 characters and keep the real value in the deployment secret store.
MAINTAINER_EDIT_TOKEN=replace-with-a-long-random-secret
